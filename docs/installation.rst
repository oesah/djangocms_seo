============
Installation
============

At the command line::

    $ easy_install djangocms-seo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangocms-seo
    $ pip install djangocms-seo
